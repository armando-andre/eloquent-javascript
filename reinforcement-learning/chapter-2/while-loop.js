// Create a variable number and initialize it to 1.
// Write a while loop that continues as long as number is less than or equal to 5.
// Inside the loop, print the current value of number and then increment it by 1 (number = number + 1 or number++).

let number = 1;

while (number <= 5) {
  console.log(number);
  number++;
}
