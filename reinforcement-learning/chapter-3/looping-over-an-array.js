// Create an array of numbers, for example: let numbers = [1, 5, 10, 15, 20];
// Write a for...of loop that goes through each number in the array and prints it to the console.

let number = [1, 5, 10, 15, 20];

for (let element of number) {
  console.log(element);
}
