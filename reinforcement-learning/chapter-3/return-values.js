// Write a function called calculateArea that takes two parameters: width and height.
// The function should multiply the width and height and return the result.
// Call the function with some numbers and store the returned value in a variable.
// Print the variable to the console to show the calculated area.

function calculateArea(width, height) {
  return width * height;
}

const total = calculateArea(5, 5);

console.log(total);
