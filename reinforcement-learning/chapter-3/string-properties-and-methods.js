// Create a string variable, for example: let message = "Hello World";
// Print the length of the string.
// Use the slice method to get just the word "Hello" from the string and print it.

let message = "Hello World";

console.log(message.length);

console.log(message.slice(0, 5));
