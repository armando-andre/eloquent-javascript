// Define a function named sayHello that takes one parameter, name.
// Inside the function, print a greeting to the console, like "Hello, [name]!". For example, if the name is "Bob", it should print "Hello, Bob!".
// Call the function a couple of times with different names.

function sayHello(name) {
  console.log("Hello " + name + "!");
}

sayHello("Bob");
sayHello("Sean");
sayHello("Matt");
