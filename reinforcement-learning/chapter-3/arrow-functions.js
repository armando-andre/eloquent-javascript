// Rewrite the calculateArea function from Exercise 2 using the arrow function syntax. It should work exactly the same way.

calculateArea = (width, height) => {
  return width * height;
};

const total = calculateArea(5, 5);

console.log(total);
