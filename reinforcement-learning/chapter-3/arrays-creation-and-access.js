// Create an array named colors that contains three of your favorite colors as strings.
// Print the first color in the array to the console.
// Print the third color in the array to the console. (Remember that array indices start at 0!)

let colors = ["blue", "white", "black"];

console.log(colors[0]);
console.log(colors[2]);
