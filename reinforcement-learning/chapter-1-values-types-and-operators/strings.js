// Create two string variables, one for a first name and one for a last name. Combine them to create a full name and print it to the console.

const firstName = "John",
  lastName = "Doe",
  fullName = firstName + " " + lastName;

console.log(fullName);
