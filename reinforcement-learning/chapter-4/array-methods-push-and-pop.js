// Start with the colors array from the previous exercise.
// Use the push method to add a new color to the end of the array.
// Use the pop method to remove the last color from the array.
// Print the final colors array to see the result.

let colors = ["blue", "white", "black"];

colors.push("gray");
colors.pop(-1);

console.log(colors);
